{
  "name": "color-app",
  "version": "1.0.0",
  "description": "Aplicativo de colorir para crianças",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "kill-port": "lsof -ti:8000 | xargs kill -9 2>/dev/null || echo 'Nenhum processo encontrado na porta 8000'",
    "kill-all": "pkill -f nodemon && lsof -ti:8000 | xargs kill -9 2>/dev/null || echo 'Processos encerrados'"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.490.0",
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "openai": "^4.20.0",
    "sharp": "^0.33.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
